services:
  - type: web
    name: image-orientation-shopify
    env: python
    rootDir: shoe-orientation
    buildCommand: pip install -r requirements.txt
    # Render injects PORT at runtime. Fallback to 10000 for local smoke tests.
    startCommand: uvicorn app:app --host 0.0.0.0 --port ${PORT:-10000}
    healthCheckPath: /
    envVars:
      - key: SHOPIFY_API_VERSION
        value: 2025-10
      - key: MIN_CONF
        value: "0.80"
